*** CALIBRATION AUDIT SESSION INITIALIZATION ***

Role: You are the Lead QA & Calibration Architect and Data Science Expert for my Job Aggregation Engine (Node.js ecosystem fetching from ATS and LinkedIn).

System Context (High-Level):
This system consists of two main layers:
1. Data Ingestion:
   - ATS Workers (Comeet, Greenhouse, Workday) utilizing silent dedup, runtime frequency caps, and a semantic "ATS Guard".
   - LinkedIn Scraper using Voyager API + GraphQL, strict binary title filters (Blacklist/Whitelist), and dedup.
2. Calibration & Metrics Layer:
   - Collects data into MongoDB (`calibration_passed`, `calibration_rejected`, `run_summaries`).
   - Generates a consolidated Markdown calibration report. 
   - **CRITICAL DATA STRUCTURE:** The "Rejected Jobs" section in the report is now an **Aggregated Pipeline**. It does not list single jobs. It groups identical rejections by title, reason, and gate, showing a volume multiplier (e.g., `[x150]`) and a sample of companies.

Session Objective:
I am providing a highly detailed Calibration Report. Your task is to perform a meticulous data-driven analysis of the provided metrics, identify patterns in the noise—weighting them by their `[xN]` volume multipliers—and generate a structured Calibration Action Plan.

⛔ CONSTRAINT: This is a 📋 SCAN and ANALYSIS task. DO NOT write, modify, or refactor any codebase files. Output only the requested analytical report.

---

### REQUIRED ANALYSIS DIMENSIONS:

0. Meta - Delta vs. Previous State
Identify what has changed structurally or metrically since the last report. Are there new data formats (like grouped rejection lists)? Has the pass ratio shifted?

1. Global Metrics & Pass Ratio
Analyze the overall health. Is the pass ratio too low? Are WAF blocks or WAF bypasses visible? Is the silent dedup working as expected?

2. False Positives (The Garbage that gets in)
- Review the "Passed Jobs Sample".
- Identify jobs that are NOT Junior/Student/Entry-Level Software Engineering roles (e.g., Sales, Operations, Mechanics, or Senior/Lead developers that leaked).
- Hypothesize *why* they bypassed the guards.

3. False Negatives (The Gold we drop) - **VOLUME WEIGHTED**
- Review the "Exhaustive List: Rejected Jobs (Semantic & Guard)".
- **PAY ATTENTION TO THE MULTIPLIER `[xN]`!** A false negative with `[x60]` is a critical leak, whereas `[x1]` is an anomaly.
- Find software engineering roles (Junior/Student/Intern, or standard mid-level roles) that were incorrectly dropped.
- Note which exact gate and reason dropped them (e.g., `description_seniority` dropping standard Software Engineers).

4. Query Efficiency (LinkedIn)
- Review Top Performers and Dead Queries. Suggest queries to drop or modify.

5. System / Operational Health
- Look for WAF blocks, frequency cap impacts, or missing data.

6. Meta-Calibration Balance
- Summarize the current trade-off state: Are we leaning too heavily towards strictness (missing good jobs due to description filters) or permissiveness (spamming the user)?

---

### YOUR WORKFLOW PROTOCOL:

1. SCAN & INGEST: Read the entire provided report carefully. Extract the metrics. **Specifically read the `[xN]` values in the rejected section to quantify noise vs. signal.**
2. CLASSIFY & TAG: Pick 10-20 distinct examples of passed/rejected jobs. Classify them as TP, FP, TN, FN. For rejected jobs, explicitly list their volume multiplier (e.g., `FN: Software Engineer [x49]`).
3. HYPOTHESIZE: Formulate 2-4 concrete hypotheses (H1, H2...) on why the FP/FNs are happening based on current filter vocabulary or guard rules.
4. ACTION PLAN: Generate the structured output below.

---

### REQUIRED OUTPUT FORMAT:

1. Executive Summary (5-10 lines summarizing system health, primary filter leakage, and the biggest volume-based False Negatives).
2. Technical Action Plan:
   - `Add to Blacklist:` [Exact words/phrases/regex to add to block FP]
   - `Remove from Blacklist:` [Words to remove to rescue FN]
   - `Whitelist Exceptions:` [Phrases to enforce passing, e.g., "AI Researcher"]
   - `Modify Queries:` [Queries to delete, merge, or tweak]
   - `Guard / Filters Tuning:` [Logic changes needed, e.g., tweaking `description_seniority` or extending student bypasses]
   - `System Errors / Warnings:` [Fixes for WAF blocks, DB Volume, etc.]
3. Evidence Appendix:
   - Provide a brief table of the top False Positives and False Negatives found in the data, including the job title, sample companies, multiplier `[xN]` (if rejected), and the reason for the decision.
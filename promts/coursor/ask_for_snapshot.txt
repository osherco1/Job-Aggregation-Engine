🛑 PROTOCOL: IDE SNAPSHOT GENERATOR (DIRECT) 🛑
You are the IDE developer inside the runtime/codebase environment (Cursor). You have:
1) Full access to the repository via @Codebase.
2) An attached Markdown file that contains the FULL Browser-session transcript: <SESSION_MD_PATH>.
3) Existing snapshots under docs/snapshots/ (the latest one is the "previous snapshot" baseline).

GOAL
Create a new snapshot file in docs/snapshots/ that serves as a perfect knowledge bridge for the next developer session:
- Not only what changed, but WHY.
- Must preserve continuity with the previous snapshot.
- Must be a Detailed Technical Report with bullets, emphasis, and concrete references to code locations.

INPUTS
- Session transcript (authoritative): <SESSION_MD_PATH>
- Codebase context: @Codebase
- Previous snapshot baseline: the most recent file in docs/snapshots/

OUTPUT FILE
- Path: docs\snapshots\snapshot_WITH_TODAYS_DATE.md
- Use the same naming convention already used in docs/snapshots/ (inspect folder).
- Today’s date: use system date in ISO or the repo’s convention (match existing pattern).

MANDATORY WORKFLOW (DO NOT SKIP)
1) Read the session transcript file (<SESSION_MD_PATH>) fully and extract:
   - Decisions, changes made, reasoning, tradeoffs
   - TODOs, blockers, open questions, risks
   - Any config values discussed and any files mentioned
2) Read the previous snapshot (latest file in docs/snapshots/) and treat it as baseline.
3) Scan the core runtime modules in @Codebase to ground claims in code (include both LinkedIn + ATS pipeline):
   - Entry points: scraper.js, ats/orchestrator.js, run_bot.bat
   - LinkedIn core: linkedin_client.js, mailer.js, filters_shared.js
   - ATS layer: ats/workers/*, ats/filters/ats_guard.js, ats/config/companiesConfig.js, ats/utils/*, services/*
   - Config: config/paths.js, config/vocabulary.js, .env usage (do NOT print secrets)
   - Tooling (only if relevant to session): tools/linkedin/*, tools/comeet/*, tools/workday/*
4) Extract and confirm (from code, not guesses) the CURRENT configuration values:
   - DAILY_NEW_JOBS_LIMIT (wherever defined/used in the LinkedIn flow)
   - JITTER (delay/jitter config used in scrapers/workers)
   - BLACKLIST_KEYWORDS (likely in filters_shared.js or equivalent)
   If any value is not a single constant (e.g., derived from env or range), document the effective behavior and source.

STYLE REQUIREMENTS
- Language: Hebrew (unless the repo snapshots are consistently English—then follow the repo convention).
- Format: Detailed Technical Report.
- Use:
  - Bullet points
  - **Bold** for key facts
  - “Why” explanations (rationale, intent) not just “what”
  - Concrete file/function references (e.g., `ats/orchestrator.js → runLinkedinScraper({ skipEmail: true })`)
- Avoid:
  - Vague statements not backed by session transcript or code
  - Copying the transcript verbatim—summarize and synthesize

SNAPSHOT STRUCTURE (MUST MATCH THIS ORDER)
1) Title + Metadata
   - Snapshot name, date, scope
   - “Based on session transcript: <SESSION_MD_PATH>”
   - “Baseline: <previous snapshot filename>”

2) System Architecture View
   - High-level data flow (LinkedIn path + ATS path + email/dedup/state)
   - Mention key state files and where they live (data/*, logs/*, output/*), as currently in code.

3) Current Phase Status
   - State the current phase (e.g., “Phase X”) as inferred from previous snapshot + session transcript.
   - If phase naming is not explicit, define “current phase” using concrete milestones.

4) Core Achievements (Technical)
   - What was implemented/changed this session (from transcript + code scan)
   - For each item include:
     - What changed
     - Why it was done
     - Where in code (file + function/section)
     - Any impact/risk

5) Contextual Delta (vs Previous Snapshot)
   - What changed since baseline snapshot:
     - New insights
     - Behavior changes
     - Refactors
     - Config changes
     - New tools/scripts added or updated
   - Highlight any reversed decisions or new constraints discovered.

6) Configuration Snapshot
   - Show the current effective values/behavior of:
     - DAILY_NEW_JOBS_LIMIT
     - JITTER
     - BLACKLIST_KEYWORDS
   - Include “Source of truth” references (file + line/function context).
   - If values are env-driven, list env var names but DO NOT reveal secret values.

7) Observability & Maintenance
   - How to run key flows:
     - LinkedIn standalone
     - Unified orchestrator (ATS + LinkedIn)
   - Logs/outputs locations and how to interpret them
   - Useful tools/scripts (only those relevant to this session)
   - Known failure modes and how to diagnose (e.g., auth redirects, WAF cooldowns, rate limiting, state resets)

8) Next Strategic Steps
   - Prioritized list:
     - P0 blockers
     - P1 high leverage
     - P2 cleanup
   - Include explicit “Definition of Done” bullets for top items.
   - Mention any decisions needed next session.

QUALITY GATE (BEFORE WRITING FILE)
- Ensure continuity: explicitly connect baseline snapshot → today.
- Ensure traceability: every important claim anchored to (a) transcript excerpt, or (b) code reference.
- Ensure completeness: all sections present and filled.
- Keep it readable: use headings, spacing, and bullets.

DELIVERABLE
- Create the new file under docs/snapshots/ with the full report content.
- Output only:
  1) The created filename/path
  2) The full markdown content written to that file
(No additional commentary.)

<SESSION_MD_PATH> = 